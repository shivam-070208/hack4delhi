# Example environment file - DO NOT commit your real secrets here
# Copy this to `.env.local` (or set env vars in your deployment) and fill values.

# NextAuth / AuthJS
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# MongoDB (connection string)
MONGODB_URI=your_mongodb_connection_string_here

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Email provider (SMTP)
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your_email_address_here
EMAIL_SERVER_PASSWORD=your_email_password_or_app_password_here
EMAIL_FROM="Your App <no-reply@example.com>"

# Optional (recommended):
# NEXTAUTH_URL should match the URL registered in your OAuth provider
# For Google OAuth redirect URI: http://localhost:3000/api/auth/callback/google
